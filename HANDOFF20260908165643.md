# Things3 Sync MVP 交接文档

> 交接时间：2026-09-08 16:56:43
> 目标读者：完全没有本次会话上下文的新对话

## 1. 我们在做什么

这是一个运行在 macOS 上的 Things3 -> Git Markdown 单向同步 MVP。目标数据流：

```text
Things3 数据库（只读）
  -> things.py 读取接口
  -> 不可变 ThingsSnapshot
  -> 确定性 Markdown 渲染
  -> 原子文件协调与 manifest
  -> 临时 Git index 隔离提交/推送
  -> 用户指定的 Git 仓库
```

设计规格、实施计划和 SDD 账本：

- `docs/superpowers/specs/2026-09-04-things3-sync-mvp-design.md`
- `docs/superpowers/plans/2026-09-04-things3-sync-mvp.md`
- `.superpowers/sdd/2026-09-04-things3-sync-mvp/progress.md`

本项目按 Task 逐项实施。每个 Task 要先记录 BASE，再进行实现、自测、独立 specification compliance review 和独立 code quality review；Critical/Important finding 必须修复并重新做范围复审，Minor finding 必须记录 deferred 或修复。

## 2. 当前仓库和工作区

- 当前分支：`main`
- 当前 HEAD：`aea2346`，与 `origin/main` 相同
- 当前 HEAD 提交：`docs: record sync task commits`
- 当前会话没有创建提交，也没有 push
- 不要清理既有嵌套 worktree 或交接文件

当前 `git status --short` 预期为：

```text
? .claude/worktrees/agent-a36ccd16e4f5b824a
? .claude/worktrees/agent-aa62f5662892e5e23
? .claude/worktrees/things3-sync-mvp-impl
 M src/config.py
 M src/sync_engine.py
 M tests/test_config.py
 M tests/test_sync_engine.py
?? HANDOFF20260907230516.md
?? HANDOFF20260908165643.md
?? main.py
?? src/service.py
?? src/watcher.py
?? tests/test_service.py
?? tests/test_watcher.py
```

说明：三个 `.claude/worktrees/*` 是已有嵌套 worktree 条目，不是本次功能文件；两个 `HANDOFF*` 文件也属于交接状态，不要删除、重置或带入功能提交。

最近相关提交：

```text
aea2346 docs: record sync task commits
d758b28 feat: coordinate snapshot sync transactions
c594ec1 fix: prevent push after isolated commit failure
242aa2b add:HANDOFF20260906203616.md
b68f72b 已按 macOS/POSIX 路径规则通过审查并记录
d4a17d7 feat: render deterministic task markdown
0d2a875 feat: render deterministic task markdown
487fab0 feat: read Things snapshots safely
abb26d5 feat: add sync models and configuration
```

## 3. 已完成内容

### Task 1：模型和配置

已完成并 review clean，提交 `abb26d5`。包括不可变内部模型、配置解析、路径清理和依赖基础配置。

### Task 2：只读 Things 适配器

已完成并 review clean，提交 `487fab0`。只调用 `things.py` 读取 API，规范化稀疏字典，支持现代数据库路径发现和完整快照重试。

### Task 3：Markdown 合同

已完成并 review clean，提交 `0d2a875`。确定性任务区块、状态标记、UUID、支持字段、多行 Notes 和 checklist 渲染已实现。

### Task 4：Snapshot Routing and Deterministic Rendering

已完成并 review clean，相关实现提交于 `d4a17d7`，并按 macOS/POSIX 路径语义完成复审。标题反斜杠是普通字符，不按 Windows 分隔符处理，这是用户已明确选择的规则。

### Task 5：Atomic File Reconciliation and Manifest

已完成并 review clean。包括原子写入、manifest 严格校验、ownership preflight、陌生文件保护、路径 containment 和明确 ChangeSet。

### Task 6：Container Rename and Persistent Archive Behavior

已完成并 review clean。包括 Project/Area 重命名、持久 Archived 历史、含陌生文件时的保守归档。两个 Minor finding 已按账本记录为 deferred：`_remove_empty_container_directories` 的提示语义和未使用的 `active_previous_paths`。

### Task 7：Explicit-Path Git Operations

已完成并 review clean。提交 `c594ec1` 及后续 `d758b28`/`aea2346` 相关提交已记录。Git 使用明确 pathspec、临时 `GIT_INDEX_FILE` 隔离提交、pending-push ref、凭据脱敏和超时控制；不会自动 pull/merge/rebase。

### Task 8：Sync Engine Transaction Boundary

核心实现已提交 `d758b28`，原始 review clean。当前工作区另有一项 post-review 隐私修正尚未提交：

- `src/sync_engine.py`：失败日志不再输出原始异常，改为固定的 `error=present`
- `tests/test_sync_engine.py`：加入包含 `/private/TARGET/...password=SECRET` 的异常消息回归断言

### Task 9：SQLite/WAL/SHM Watcher

实现位于未提交的：

- `src/watcher.py`
- `tests/test_watcher.py`

已完成三轮 review 修正：

1. false 返回结果每个通知周期最多一次 retry，避免无限重试。
2. watcher 生命周期和 debounce 异常日志不输出异常文本或 traceback；修复 start/stop 注册边界和 live observer 重试清理。
3. 增加 `_observer_start_attempted`，`observer.start()` 尚未调用时跳过 `join()`，同时保留部分启动后的清理。

Task 9 之前的独立 specification compliance 和 code quality review 均 PASS。历史测试记录：watcher 28 passed，watcher/sync/Git focused 50 passed，完整项目当时 114 passed，compileall 和 diff check 通过。

### Task 10：Service Lifecycle and Main Entry Point

实现位于未提交的：

- `src/service.py`
- `main.py`
- `tests/test_service.py`

此前已完成的行为：

- 启动时先做一次完整同步。
- 启动同步成功且未收到 stop 才启动 watcher。
- 周期同步复用 `engine.sync()`。
- `SIGINT`/`SIGTERM` 只调用 `service.stop()`，不在信号处理器中执行同步或 Git。
- watcher 先停止，随后 `debouncer.stop(wait=True)`。
- 配置阶段和运行阶段日志均使用固定消息，不泄露异常详情或敏感路径。
- watcher 启动失败时仍执行清理，但清理异常不覆盖原始启动异常。
- 正常退出时 cleanup 异常向上抛出，使 `main()` 返回 exit code 1。

本次会话已针对上一轮规格复审的三个 finding 完成工作区修正：

1. `src/config.py` 引入 `math.isfinite`，拒绝 `nan`、`inf` 和 `-inf` 等非有限配置值。
2. `src/service.py` 的 `stop()` 不持有业务调用期间的锁；启动同步、watcher 启动和周期同步之后均重新检查 stop 状态，避免停止请求导致额外 watcher/同步。
3. cleanup 失败不再在正常退出时静默吞掉；启动异常路径仍优先保留原始异常。

相关回归测试已经加入 `tests/test_config.py` 和 `tests/test_service.py`。

## 4. 当前验证结果

本次会话最后实际运行：

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -B -m pytest tests/test_config.py tests/test_service.py tests/test_watcher.py tests/test_sync_engine.py -q -p no:cacheprovider
```

结果：

```text
69 passed in 0.76s
```

以下检查也通过：

```bash
git diff --check
```

之前 Task 10 初始实现的独立 review 结果：

- specification compliance：FAIL
  - 高：配置接受 `nan/inf`
  - 高：服务在部分业务调用期间持有停止锁，停止竞态不符合规格
  - 中：cleanup 异常被吞，可能导致 `main()` 错误返回 0
- code quality：PASS，无 Critical/Important/Minor finding

上述三个规格问题现在已经在工作区修正并有回归测试，但**修正后的独立范围复审尚未执行**。因此不要把 Task 10 标记为 review clean，也不要开始 Task 11。

完整项目测试在修正前由复审记录为：

```text
PYTHONPATH=things.py:. ... pytest tests -q：134 passed
```

修正后的完整测试、compileall 和禁止操作扫描尚未在本次会话最后阶段重新执行。

## 5. 当前卡点

唯一正式流程卡点是：Task 10 的三个 finding 虽已修复，尚未完成修复后独立的 specification compliance 和 code quality 双 verdict。

另外，Task 8 post-review correction、Task 9、Task 10 都仍在当前工作区未提交。不能把它们误认为已经进入 Git 历史。

## 6. 下一步计划

### 第一步：检查修复后的实际 diff

读取并检查：

- `src/config.py`
- `tests/test_config.py`
- `src/service.py`
- `tests/test_service.py`
- `src/sync_engine.py`
- `tests/test_sync_engine.py`
- `src/watcher.py`
- `tests/test_watcher.py`

确认没有实现代理引入的无关改动、敏感信息或破坏既有测试的行为。

### 第二步：完成 Task 10 范围复审

基于修复后的精确文件范围，重新派发独立 reviewer，同时要求分别给出：

- specification compliance verdict
- code quality verdict

如有 Critical/Important finding，最多按既定流程进行 5 轮修复/范围复审；不要把实现代理自测当正式 review。若 review agent 因 403、504 或响应解码问题失败，要记录真实失败原因并重新派发，不能虚构 verdict。

### 第三步：更新 SDD 账本

只有修复后双 verdict 都 clean 后，才能在：

```text
.superpowers/sdd/2026-09-04-things3-sync-mvp/progress.md
```

追加 Task 10 complete，并记录最终测试和 review evidence。当前账本还停留在 Task 9 complete，不能提前修改成 Task 10 complete。

### 第四步：提交前验证

按显式文件路径分别检查 Task 8/9/10 的未提交内容：

```bash
git status --short
git diff --stat
git diff --cached --name-status
git show --name-status HEAD
```

用户此前的流程要求是先确认是否现在创建功能提交。若用户确认提交，Task 8、Task 9、Task 10 应按独立逻辑单元使用 `git commit --only`，严格采用 Conventional Commits，并只暂存明确路径。不要把 HANDOFF 文件、nested worktree 状态、`.DS_Store`、`.gitignore` 或其他无关状态带入功能提交。未经用户明确要求，不要 push、创建 PR 或加载 launchd。

### 第五步：继续 Task 11 以后

Task 10 clean 并按流程提交后，再按计划顺序执行：

1. Task 11：`setup.py`、`generate_launchd.py` 和测试。
2. Task 12：临时 Git 仓库端到端集成测试。
3. Task 13：README 与 `Agent.md` 对齐。
4. Task 14：compileall、完整测试、禁止操作扫描、whole-branch review 和最终 handoff。

## 7. 绝对不要踩的坑

### Git 和工作区

- 不要 `git reset --hard`、rebase、amend、force push、删除分支或清理工作区。
- 不要 `git clean -fdx`；尤其不能清理 `.superpowers/sdd/`、`.claude/worktrees/`、`.DS_Store` 或交接文件。
- 不要使用 `git add .`，必须显式列出路径。
- 不要把未跟踪交接文件或 nested worktree 条目放入功能提交。
- 提交前检查 `git diff --cached --name-status`，确认没有 `.env`、凭证、token、日志或临时调试文件。
- 不要把 HEAD~1 当作 review BASE；使用各 Task dispatch 时记录的实际 BASE。
- 目前没有得到本次会话的提交/push授权；先向用户确认是否创建功能提交。

### Things3 只读边界

- 业务代码只能通过 `things.py` 的窄读取接口访问 Things。
- 不要读取或写入 Things SQLite 私有 schema。
- 不要导入或调用 `complete()`、`url()` mutation helper。
- 不要构造 `update`、`update-project` 或其他写入 URL。
- 不要把参考库文档中的 `Task`、`Project`、`Area` 当作公共类。
- 不要把 `trashed` 自动映射为 canceled。
- 不要输出公开 API 无法可靠提供的 priority、Evening、附件或重复规则字段。
- Today 必须复用 `things.today()` 语义，不能只根据 `start` 字段自行重建。
- 完整快照成功前不得修改目标仓库或运行 Git。

### 文件系统和路径

- 生产文件操作使用 `pathlib.Path`。
- 写文件必须使用同目录临时文件、flush、fsync、`os.replace()`；失败时保留原文件。
- desired destination、祖先和 manifest destination 写入前都必须做 ownership preflight。
- 绝不能替换未标记陌生文件或符号链接；悬空符号链接也算已存在的 unowned entry。
- manifest 必须严格按 version 1 schema 校验；坏 JSON、坏 UTF-8、错误类型、未知字段和危险路径要在任何写入前失败。
- 只删除 marker/manifest 明确管理的文件；陌生文件必须保留并 warning。
- 归档不能递归移动含陌生文件的整个目录；只处理已管理文件。
- 路径是 macOS/POSIX 语义；标题中的反斜杠是普通字符，不是路径分隔符。
- `Archived/` 是持久历史区，不属于普通当前文件 stale deletion 集合。

### Git 实现

- 生产同步必须使用临时 `GIT_INDEX_FILE`，不能把用户原有 staged 文件带进同步提交。
- 只执行明确 pathspec，不使用 `git add .`。
- Git 命令必须有 timeout，捕获 stderr；日志中的 URL userinfo/token/auth-token/password 必须脱敏。
- 推送失败不能自动 pull、merge、rebase 或处理冲突。
- 只根据 `refs/things3-sync/pending-push` 判断同步器待推送提交；不要因分支一般领先远端就推送用户自己的提交。
- 不要对真实远端执行 push。

### 日志隐私

- 所有错误日志使用固定消息，不得输出异常文本或 traceback。
- 任何包含数据库路径、仓库路径、token、auth-token、password 的异常消息必须留在日志外。
- 回归测试应继续使用类似 `password=/private/TARGET/...` 的异常消息，断言原始文本不出现在日志中。
- watcher/service 不使用 `logger.exception(...)`。

### 服务并发和生命周期

- `stop()` 必须能在 engine 同步或 watcher 启动阻塞时及时设置 stop event；不要把停止请求锁住在业务调用期间。
- 启动同步完成后、watcher 启动返回后、周期 wait 返回后要重检停止状态。
- 正常 cleanup 异常必须传播到 `main()`，否则会错误返回 0。
- 启动异常路径的 cleanup 不能覆盖原始启动异常；清理仍需按 watcher 后 debouncer 的顺序执行。
- debounce callback 不应直接并发执行多个同步；busy 时最多补跑一次。

### 测试和环境

- 优先使用 `/tmp/things3-task3-venv/bin/python`；系统 Python 可能没有 pytest。
- 推荐完整命令：

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -B -m pytest tests -q -p no:cacheprovider
```

- 如果本地 `things.py` 可用，使用 `PYTHONPATH=things.py:.`；未设置时可选 fixture 测试会因找不到 `things` 模块失败，这不等同于项目测试逻辑失败。
- 避免产生不必要的 bytecode/cache；使用 `PYTHONDONTWRITEBYTECODE=1 python3 -B`。
- broad pytest 可能误收集被忽略的 `things.py/tests`，出现 `ModuleNotFoundError: tests.test_things`；报告中要区分这个环境问题和项目 `tests/` 套件结果。
- 完整测试和 compileall 应在 Task 10 修复后重新运行，不能只引用修复前的 134 passed。

### SDD 和子代理

- 正式 review 必须分别给出 specification compliance 和 code quality verdict。
- 实现代理自测不等于正式 review。
- review agent 的 FAIL 必须进入修复/范围复审循环；不能仅因为 focused tests 通过就标记 clean。
- review agent 失败时记录真实原因，不虚构结论。
- 未完成 Task 10 修复后 review 前不能开始 Task 11。

## 8. 重要交接结论

当前代码工作区已经包含 Task 8 隐私修正、Task 9 watcher、Task 10 service/main 以及 Task 10 复审修复；最近一次相关 focused 测试为 69 passed。但 Task 10 修复后的独立双 verdict 尚未获得，SDD 账本也尚未更新，任何新对话应从“检查实际 diff -> 完成 Task 10 范围复审”开始，而不是直接提交或进入 Task 11。
