# Things3 Sync MVP 交接文档

> 交接时间：2026-09-07 23:05:16
> 目标读者：没有本次会话上下文的新对话

## 1. 我们在做什么

项目是一个运行在 macOS 上的 Things3 -> Git Markdown 单向同步服务。目标数据流：

```text
Things3 数据库（只读）
  -> things.py 读取接口
  -> 不可变 ThingsSnapshot
  -> 纯确定性 Markdown 渲染
  -> 原子文件协调与 manifest
  -> 临时 Git index 隔离提交/推送
  -> 用户指定的 Git 仓库
```

设计与实施计划位于：
- `docs/superpowers/specs/2026-09-04-things3-sync-mvp-design.md`
- `docs/superpowers/plans/2026-09-04-things3-sync-mvp.md`

SDD 账本和所有任务 brief/report/review 位于：
- `.superpowers/sdd/2026-09-04-things3-sync-mvp/progress.md`

工作规则是按任务执行：每个任务先记录 BASE，派发 implementer，再做独立的 specification compliance 和 code quality review；有 Critical/Important finding 时由 implementer 修复，再做范围复审。Minor finding 记录为 deferred 或修复。

每次回复我先称呼我为妹妹

## 2. 当前分支和工作区

单元测试通过后立即执行原子 commit 并 push；后续复审发现问题则单独提交 fix commit，严禁等待复审而堆积大量未提交修改。

## Git Workflow Rules
1. **Atomic Commits**: 在完成一个独立的逻辑单元且测试/类型检查通过后，主动创建原子化 Git 提交。
2. **Commit Message Format**: 必须严格遵循 Conventional Commits 规范（如 `feat:`, `fix:`, `refactor:`, `test:`）。
3. **Staging**: 仅暂存本次修改涉及的文件，严禁盲目执行 `git add .`。
4. **Safety Restrictions**:
   - 提交前检查代码，严禁将 `.env`、API Keys、密钥或临时调试文件（如 `.log`）提交进版本库。
   - 自动执行 `git push` 操作。

当前分支：`main`
当前 HEAD：`aea2346`（`origin/main` 同一提交）

最近提交：
```text
aea2346 docs: record sync task commits
d758b28 feat: coordinate snapshot sync transactions
c594ec1 fix: prevent push after isolated commit failure
242aa2b add:HANDOFF20260906203616.md
b68f72b 已按 macOS/POSIX 路径规则通过审查并记录
d4a17d7 feat: render deterministic task markdown
0d2a875 feat: render deterministic task markdown
487fab0 feat: read Things snapshots safely
abb26d5 feat: add sync models and configuration
45df0d6 docs: plan Things3 sync MVP
a839e1d docs: design Things3 sync MVP
564c30f first commit
```

当前 `git status --short` 的重要状态：
```text
 M src/sync_engine.py
 M tests/test_sync_engine.py
?? main.py
?? src/service.py
?? src/watcher.py
?? tests/test_service.py
?? tests/test_watcher.py
```

另有以下已有嵌套 worktree 条目，属于既存隔离工作区，不是本次功能目标：
```text
.claude/worktrees/agent-a36ccd16e4f5b824a
.claude/worktrees/agent-aa62f5662892e5e23
.claude/worktrees/things3-sync-mvp-impl
```

`HANDOFF20260907230516.md` 与 `HANDOFF20260906203616.md` 也是既存未跟踪交接文件。不要删除、清理或重置上述内容。

重要：当前 Task 4、Task 5/6、Task 7 的实现均已提交；Task 8、Task 9、Task 10 的实现均未提交。不要假设它们已经进入 Git 历史；应先检查工作区。

## 3. 已完成内容

### Task 1：模型和配置，已完成并 review clean（commit `abb26d5`）

### Task 2：只读 Things 适配器，已完成并 review clean（commit `487fab0`）

### Task 3：Markdown 合同，已完成并 review clean（commit `0d2a875`）

### Task 4：Snapshot Routing and Deterministic Rendering，已完成并 review clean（commit `d4a17d7`）

### Task 5：Atomic File Reconciliation and Manifest，已完成并 review clean

### Task 6：Container Rename and Persistent Archive Behavior，已完成并 review clean

### Task 7：Explicit-Path Git Operations，已完成并 review clean（commit `c594ec1`、`d758b28`、`aea2346`）

### Task 8：Sync Engine Transaction Boundary，已完成并 review clean（commit `d758b28`）

Task 8 有一处 review 后的小修正（未提交）：失败日志不再输出原始异常文本，改为 `error=present`；测试用例也加入了含敏感路径的异常消息作为回归断言。

### Task 9：SQLite/WAL/SHM Watcher，已完成并 review clean（未提交）

`src/watcher.py` 与 `tests/test_watcher.py`。经历三轮独立 review 修复：
- 第 1 轮：false 结果导致无限重试；改为每个通知周期最多一次 retry。
- 第 2 轮：watcher 生命周期错误日志泄露异常文本；改为固定消息且不输出 traceback；同时修复 start/stop 注册边界、live observer 重试清理。
- 第 3 轮：`observer.start()` 未调用时 `join()` 会把可恢复状态卡死；新增 `_observer_start_attempted` 标记，未启动时跳过 join。

最终 specification compliance PASS、code quality PASS。watcher 测试 28 passed，focused watcher/sync/Git 50 passed，完整项目 114 passed。

### Task 10：Service Lifecycle and Main Entry Point，已实现，最终 review 未完成（未提交）

`src/service.py`、`main.py`、`tests/test_service.py`。经历两轮独立 review 修复：
- 第 1 轮：配置阶段只捕获 `ValueError`；周期同步与顶层运行日志泄露异常文本。改为配置阶段统一返回 exit code 2，所有日志使用固定消息。
- 第 2 轮：`watcher.start()` 失败时未进入 cleanup；`dotenv.load_dotenv()` 不在配置边界内；初始同步期间收到 stop 仍会启动 watcher；`wait()` 后未重检 stop 导致多执行一次同步；watcher 清理异常掩盖原始启动失败。全部修复。

当前服务测试 20 passed，完整项目 134 passed，`compileall` 与 `git diff --check` 通过。

## 4. 当前卡点

唯一明确的流程卡点是 Task 10 的最终独立 review gate 尚未完成。

最近一次最终 review（规格 + 质量双 verdict）已启动但尚未返回结果。在拿到正式双 verdict 之前，不要把 Task 10 标记为 review clean，也不要进入 Task 11。

如果 review agent 再次因 API/账户问题失败，记录真实原因，不要虚构结论。

## 5. 下一步计划

### 第一步：闭合 Task 10 review

- 检查最终 review 结果。
- 如果出现 Critical/Important finding，派发 implementer 修复，再做范围复审，最多 5 轮。
- 只有 review clean 后才能在 `progress.md` 追加 Task 10 complete。

### 第二步：Task 10 提交准备

Task 10 review clean 后，分别检查各任务的未提交内容。由于 Task 8、Task 9、Task 10 都在当前 workspace，不能使用 `git add .`。

计划提交前应：
- 先确认用户是否希望现在创建功能提交；本会话之前明确要求实现代理不自行提交。
- 若提交，按任务拆分、显式列出路径并使用 `git commit --only`；不要把 `HANDOFF*`、nested worktree 条目、`.DS_Store`、`.gitignore` 或其他无关状态带入。
- 提交前检查 `git status --short`、`git diff --cached --name-status`、`git show --name-status HEAD`。

### 第三步：Task 11 及以后

按计划文件顺序：
- Task 11：setup.py 和 launchd plist preview。
- Task 12：临时 Git 仓库端到端集成测试。
- Task 13：README 与 Agent.md 对齐。
- Task 14：compileall、全套测试、禁止操作扫描、whole-branch review。

每个任务必须有 brief、记录 BASE、implementer report、review package 和独立双 verdict review。

## 6. 绝对不要踩的坑

### Git 和分支

- 当前在 `main`，不要擅自新建嵌套 worktree，也不要切换到旧实现 worktree。
- 不要 `git reset --hard`、rebase、amend、force push、删除分支或清理工作区。
- 不要使用 `git clean -fdx`，尤其不能清理 `.superpowers/sdd/`、`.claude/worktrees/` 或 `.DS_Store`。
- 不要使用 `git add .` 或不带明确路径的 commit。
- 不要把无关的 `.gitignore`、`docs/CHANGELOG.md`、`.DS_Store`、HANDOFF 文件或 nested worktree 状态带入功能提交。
- 不要把 `HEAD~1` 当 task review BASE；必须使用实际 dispatch 前记录的 BASE。
- 未经用户明确要求，不要 push、创建 PR、修改远程或加载 launchd。

### SDD 流程

- implementer 自测不是正式 review；正式 review 必须分别给出规格合规和代码质量 verdict。
- reviewer 的 Critical/Important finding 必须进入修复循环；controller 不直接修改相关实现。
- 修复后必须重新生成 scoped package 和独立复审，不能因改动很小而跳过。
- Minor finding 不能静默丢弃；应记录为 deferred 或修复。
- review agent 失败后不要虚构结论；重新派发并记录真实原因。
- 未完成 Task 10 review 前不能开始 Task 11。

### Things3 只读边界

- 业务代码只能通过 `things.py` 的窄读取接口访问 Things。
- 不要读取/写入 Things SQLite 私有 schema。
- 不要导入或调用 `complete()`、`url()` mutation helper。
- 不要构造 `update`、`update-project` 或其他写入 URL。
- 不要把 `things.py` 文档中的 `Task`、`Project`、`Area` 当作公共类。
- 不要把 `trashed` 自动映射为 canceled。
- 不要输出公开 API 无法可靠提供的 priority、Evening、附件或重复规则字段。
- Today 必须复用 `things.today()` 语义；不能只根据 `start` 字段自行重建。
- 完整快照读取成功之前，不得修改目标仓库或运行 Git。

### 文件系统和路径

- 生产文件操作使用 `pathlib.Path`。
- 写文件必须使用同目录临时文件、flush、fsync、`os.replace()`；失败时保留原文件。
- 任何 desired destination、祖先、manifest destination 在写入前都要做 ownership preflight。
- 绝不能替换未标记陌生文件或符号链接；悬空符号链接也算已存在的 unowned entry。
- manifest 必须严格按 version 1 schema 校验；坏 JSON、坏 UTF-8、错误类型、未知字段、危险路径都要在任何写入前失败。
- 只删除 marker/manifest 明确管理的文件；陌生文件必须保留并 warning。
- 归档不能递归移动含陌生文件的整个目录；只处理已管理文件。
- 路径必须是相对路径，禁止绝对路径和 `..`；本项目路径语义明确采用 macOS/POSIX，标题反斜杠是普通字符。
- `Archived/` 是持久历史区，不属于普通当前文件 stale deletion 集合。

### Git 实现

- 生产同步必须用临时 `GIT_INDEX_FILE`，不能把用户原有 staged 文件带进同步提交。
- 只使用明确 pathspec，不要 `git add .`。
- Git 命令必须有 timeout、捕获 stderr，日志中的 URL userinfo/token/auth-token/password 必须脱敏。
- 推送失败不能自动 pull、merge、rebase 或处理冲突。
- 只根据 `refs/things3-sync/pending-push` 判断同步器待推送提交，不要因为分支一般领先远端就推送用户自己的提交。
- 不要对真实远端执行 push。

### 日志隐私（本次会话反复踩到）

- 所有错误日志必须使用固定消息，不得输出异常文本或 traceback。
- 任何包含数据库路径、仓库路径、token、auth-token、password 的异常消息都必须在日志外出现。
- 测试中要用含 `password=` 或 `/private/TARGET/...` 的异常做回归断言，证明敏感文本不在日志中。
- `logger.exception(...)` 在 watcher/service 代码中一律不用；用 `logger.error("固定消息")`。

### 测试和环境

- 优先使用 `/tmp/things3-task3-venv/bin/python`，系统 Python 可能没有 pytest。
- 推荐命令：
```bash
/tmp/things3-task3-venv/bin/python -m pytest tests -q -p no:cacheprovider
```
- 如果执行根目录 broad `pytest`，可能误收集被忽略的 `things.py/tests`，出现 `ModuleNotFoundError: tests.test_things`；这不是项目 `tests/` 套件失败。报告中要区分两者。
- 测试时避免产生不必要的 bytecode/cache；可用 `PYTHONDONTWRITEBYTECODE=1 python3 -B -m pytest -p no:cacheprovider ...`。
- 每次任务的报告要记录 RED/GREEN 命令、精确输出和未执行的检查。

### Workflow / 子代理

- 本项目使用 Workflow 工具派发 implementer 与独立 reviewer，脚本必须是纯 JavaScript，不能用 TypeScript 语法。
- 脚本内的 agent prompt 如果包含反引号等特殊字符，会被解析器拒绝；用普通引号或拆分字符串。
- review agent 与 implementer 都可能因 `403 Insufficient account balance`、`504 openai_error` 或响应流解码错误而失败。失败时不要虚构结论，记录真实原因后重试，必要时改用更低成本模型（haiku）。
- 不要把实现代理自测当正式 review；正式 review 必须分别给出规格合规和代码质量 verdict。

## 7. 继续开发的启动步骤

新对话应按以下顺序操作：

1. 阅读本文件。
2. 阅读 `Agent.md`、设计规格、实施计划和 `.superpowers/sdd/2026-09-04-things3-sync-mvp/progress.md`。
3. 执行 `git status --short`、`git log --oneline --decorate -10`，确认当前 HEAD 与未提交文件；不要清理任何既有状态。
4. 检查 Task 10 最终 review 结果；若未完成，重新派发独立 reviewer。
5. Task 10 review clean 后，更新账本，再开始 Task 11。
6. 后续每个任务严格执行 brief -> BASE -> implementer -> report -> review -> fix/re-review -> ledger 的顺序。

本交接文件不属于任何功能提交，除非用户明确要求提交它。