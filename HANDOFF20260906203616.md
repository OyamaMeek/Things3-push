# Things3 Sync MVP 交接文档

> 交接时间：2026-09-06 20:36:16
> 目标读者：没有本次会话上下文的新对话

## 1. 我们在做什么

当前项目是一个运行在 macOS 上的 Things3 -> Git Markdown 单向同步服务。目标数据流：

```text
Things3 数据库（只读）
  -> things.py 读取接口
  -> 不可变 ThingsSnapshot
  -> 纯确定性 Markdown 渲染
  -> 原子文件协调与 manifest
  -> 临时 Git index 隔离提交/推送
  -> 用户指定的 Git 仓库
```

MVP 设计和实施计划分别位于：

- `docs/superpowers/specs/2026-09-04-things3-sync-mvp-design.md`
- `docs/superpowers/plans/2026-09-04-things3-sync-mvp.md`

SDD 账本和所有任务 brief/report/review 位于：

- `.superpowers/sdd/2026-09-04-things3-sync-mvp/progress.md`

工作规则是按任务执行：每个任务先记录 BASE，派发 implementer，再做独立的 specification compliance 和 code quality review；有 Critical/Important finding 时由 implementer 修复，再做范围复审。

## 2. 当前分支和工作区

当前分支：`main`

当前 HEAD：`b68f72b`（`origin/main` 同一提交）

最近提交：

```text
b68f72b 已按 macOS/POSIX 路径规则通过审查并记录
d4a17d7 feat: render deterministic task markdown
0d2a875 feat: render deterministic task markdown
487fab0 feat: read Things snapshots safely
abb26d5 feat: add sync models and configuration
45df0d6 docs: plan Things3 sync MVP
a839e1d docs: design Things3 sync MVP
564c30f first commit
```

当前 `git status --short` 的重要状态：

```text
M  src/renderer.py
M  tests/test_renderer.py
?? src/filesystem.py
?? src/git_ops.py
?? tests/test_filesystem.py
?? tests/test_git_ops.py
```

另有以下已有嵌套 worktree 条目，属于既存隔离工作区，不是本次功能目标：

```text
.claude/worktrees/agent-a36ccd16e4f5b824a
.claude/worktrees/agent-aa62f5662892e5e23
.claude/worktrees/things3-sync-mvp-impl
```

`HANDOFF20260905060034.md` 也是既存未跟踪交接文件。不要删除、清理或重置上述内容。

重要：当前 Task 4、Task 5/6、Task 7 的实现均未提交。不要假设它们已经进入 Git 历史；应先检查工作区，再分别完成审查和提交边界处理。

## 3. 已完成内容

### Task 1：模型和配置，已完成并 review clean

提交：`abb26d5`

主要文件：

- `src/models.py`
- `src/config.py`
- `src/utils.py`
- `.env.example`
- `requirements.txt`
- `requirements-dev.txt`
- `tests/test_models.py`
- `tests/test_config.py`

已实现：不可变 `ChecklistItem`、`TaskSnapshot`、`ContainerSnapshot`、`ThingsSnapshot`、`ContainerPath`、`RenderedSnapshot`、`ChangeSet`、`SyncResult`；配置解析/校验；路径清理和日志初始化。

关键不变量：快照集合归一化为 tuple；`RenderedSnapshot.files` 是 `MappingProxyType`；重复 task/container UUID 被拒绝；状态只有 `incomplete`、`completed`、`canceled`；`AUTO_PUSH=true` 必须同时启用 `AUTO_COMMIT`。

### Task 2：只读 Things 适配器，已完成并 review clean

提交：`487fab0`

主要文件：

- `src/things_adapter.py`
- `tests/test_things_adapter.py`

已实现：只使用 `todos`、`projects`、`areas`、`today`、`upcoming` 读取接口；显式数据库路径；现代 `ThingsData-*` 路径发现；完整快照重试；稀疏字典归一化；Today/Upcoming API 顺序保留并去重；不访问 SQLite 私有实现，不调用 mutation API。

### Task 3：Markdown 合同，已完成并 review clean

实现提交：`0d2a875`

审查范围必须是 `487fab0..0d2a875`，不能用 `HEAD~1`，因为之后有不相关的 `d4a17d7`。

主要文件：

- `src/markdown.py`
- `tests/test_markdown.py`

接口：`GENERATED_MARKER`、`escape_inline()`、`render_task()`、`render_document()`、`is_managed_markdown()`。

已实现固定 marker、状态 checkbox、字段顺序、Markdown 特殊字符转义、CRLF/CR 处理、notes 缩进、tags、checklist UUID/完成时间、任务 separator 和 managed-file 检测。独立审查结论：specification compliance PASS，code quality PASS；测试 4 passed。证据：`task-3-review.md`。

### Task 4：Snapshot Routing and Deterministic Rendering，代码完成并 review clean

当前代码文件：

- `src/renderer.py`
- `tests/conftest.py`
- `tests/test_renderer.py`

接口：

```python
render_snapshot(snapshot: ThingsSnapshot) -> RenderedSnapshot
allocate_container_paths(
    projects: Sequence[ContainerSnapshot],
    areas: Sequence[ContainerSnapshot],
) -> Tuple[ContainerPath, ...]
```

已实现：

- Project > Area > Inbox/Anytime/Someday 主路由；terminal status 先于 start-list。
- 固定七个顶层文件：`Inbox.md`、`Today.md`、`Anytime.md`、`Someday.md`、`Upcoming.md`、`已完成.md`、`已取消.md`。
- 每个 active Project/Area 即使为空也生成 `tasks.md`、`已完成.md`、`已取消.md`。
- 容器名称清理、大小写不敏感冲突处理、相同 UUID 前缀和 case-only UUID 的稳定消歧。
- UUID 派生路径 token 安全化，禁止 hostile UUID 产生绝对/`..` 路径。
- Today/Upcoming 严格按 snapshot 提供的 UUID 顺序，缺失 UUID warning 一次并跳过。
- 任务输入顺序不影响结果；普通任务稳定排序；终态按 `stop_date` 降序并保持确定性 tie-breaker。
- renderer 纯内存，不读写文件、不执行 Git、不访问 Things/SQLite。

测试：renderer 12 passed；前序/项目测试合计在 Task 4 修复后 32 passed。Task 4 审查过程中发现并修复了两个 Important 问题：case-only UUID 路径冲突、UUID 派生路径未安全化。

有一个审查项被明确裁定为不适用而停放：`sanitize_path_segment()` 不把反斜杠当 Windows 分隔符。用户选择本项目采用 macOS/POSIX-only 语义，因此标题中的反斜杠是普通字符。证据和裁定在 `task-4-rereview-round-2.md` 与 `progress.md`。

### Task 5：Atomic File Reconciliation and Manifest，代码完成并 review clean

当前实现和测试在：

- `src/filesystem.py`
- `tests/test_filesystem.py`

已实现：

- `Manifest` frozen dataclass，version 1，严格顶层 schema。
- `.things3-sync-manifest.json` 安全读取、类型/版本/路径/UTF-8 校验。
- 所有 desired destination、祖先、manifest destination 在任何写入前做 ownership preflight。
- 直接文件、普通文件祖先、直接/祖先符号链接（包括悬空链接）均不能被静默替换。
- sibling temporary file、UTF-8、LF、flush、fsync、`os.replace()` 和 finally 清理。
- 内容未变化不重写；manifest 最后写入。
- 只删除旧 manifest 明确管理且仍带生成 marker 的 Markdown；陌生文件保留并 warning。
- 只沿已知容器目录做非递归空目录清理。
- `ChangeSet` 返回确定性 added/modified/deleted，并把历史 managed paths、当前 managed paths、manifest 和变更路径放入明确 `git_paths`。

Task 5 审查经历三轮修复：

1. 补齐普通文件祖先预检、严格 manifest schema、非 UTF-8 unowned destination 异常。
2. 补齐直接/祖先符号链接预检、非法 UTF-8 manifest 的 `ValueError`。
3. 补齐 manifest 本身悬空符号链接预检。

最终 specification compliance PASS、code quality PASS；文件系统测试 23 passed，项目测试当时 55 passed。证据：`task-5-rereview-round-3.md`。

### Task 6：Container Rename and Persistent Archive Behavior，代码完成并 review clean

Task 6 修改了同一个 `src/filesystem.py` 和 `tests/test_filesystem.py`，当前仍未提交。

已实现：

- 按 `<kind>:<uuid>` 识别 active container。
- active rename 只写新路径并删除旧路径下标记的 managed 文件，不移动整个目录。
- 消失的 Project/Area 分别归档到 `Archived/Projects`、`Archived/Areas`。
- 归档目录使用最后 active 名称和稳定八字符 UUID 后缀；全非字母数字 UUID 使用 SHA-256 前缀。
- archive mapping 持久化；重新出现保留历史；再次消失复用同一 archive 目录。
- 陌生文件始终保留并记录 warning；归档文件也做 marker/ownership 检查。
- 归档路径经过相对路径和仓库 containment 校验；不用 `shutil.move` 或递归目录移动。

测试：文件系统测试 32 passed；项目测试 64 passed。独立审查：specification compliance PASS，code quality PASS with two deferred Minor findings：

- `_remove_empty_container_directories()` 对某些 unchanged/nonempty active container 可能记录误导性 warning。
- `active_previous_paths` 变量计算后未使用。

这两个 Minor 已登记在 `progress.md`，未进入修复循环；最终 whole-branch review 时需要重新判断是否清理。

### Task 7：Explicit-Path Git Operations，已实现但正式 review 未完成

当前文件：

- `src/git_ops.py`
- `tests/test_git_ops.py`

已实现：

- `GitClient` 统一 subprocess 边界，所有调用带 `cwd`、`capture_output=True`、`text=True`、timeout、`check=False`。
- `git add -- <明确路径>`，没有 `git add .`。
- URL userinfo 和 `token=`、`auth-token=`、`password=` 日志脱敏。
- 临时 `GIT_INDEX_FILE`，`read-tree HEAD`，仅暂存 managed paths，使用实际 `--name-status -z` 统计 commit。
- commit 成功后只用 managed path 刷新真实 index，保留用户无关 staged changes。
- `refs/things3-sync/pending-push` 的创建、推送失败重试、成功清除。
- 不自动 pull、merge、rebase。

测试：Task 7 focused 12 passed；当时项目测试 76 passed；`py_compile` 和 `git diff --check` 通过。证据：`task-7-report.md`。

卡点：Task 7 formal independent review 尚未完成。第一次 review agent 因 API 503 失败，第二次尝试因 token quota 不足返回 403；因此没有 `task-7-review.md`，不能把 Task 7 标记为 review clean，也不能直接进入 Task 8。

## 4. 当前卡点

唯一明确的流程卡点是 Task 7 的正式 SDD review gate。

必须先完成：

1. 阅读 `task-7-brief.md`、`task-7-report.md`、`review-task-7-complete.diff`。
2. 重新派发独立 reviewer，要求分别输出 specification compliance 和 code quality verdict。
3. 如果出现 Critical/Important finding，恢复 Task 7 implementer（前 3 轮）或派发新 implementer 修复，不能由 controller 直接编辑 `src/git_ops.py`。
4. 每轮修复后生成 scoped review package 并独立复审，最多 5 轮。
5. 只有 review clean 后才能在 `progress.md` 追加 Task 7 complete。

审查代理可优先使用更低成本模型以避开当前额度问题，但仍必须是独立 reviewer，不能把实现代理自测当正式审查。

## 5. 下一步计划

### 第一步：闭合 Task 7 review

- 保持当前工作区内容不变，先验证 `git status --short`。
- 使用完整未跟踪文件内容重新生成 Task 7 review package，不能只依赖 `git diff`，因为新文件尚未 tracked。
- 独立 review Git subprocess 边界、临时 index 隔离、路径规格、凭证脱敏、pending-push 状态机和真实临时仓库测试。
- 修复 findings 后复审，更新 SDD ledger。

### 第二步：Task 7 边界检查和提交准备

Task 7 review clean 后，分别检查各任务的未提交内容。由于 Task 4、Task 5/6、Task 7 都在当前 workspace，不能使用 `git add .`。

计划提交前应：

- 先确认用户是否希望现在创建功能提交；本会话之前明确要求实现代理不自行提交。
- 若提交，按任务拆分、显式列出路径并使用 `git commit --only`；不要把 `HANDOFF*`、nested worktree 条目、`.DS_Store`、`.gitignore` 或其他无关状态带入。
- 提交前检查 `git status --short`、`git diff --cached --name-status`、`git show --name-status HEAD`。

### 第三步：Task 8 — Sync Engine Transaction Boundary

按计划文件生成 brief 并记录新的 BASE，然后只新增：

- `src/sync_engine.py`
- `tests/test_sync_engine.py`

实现读取 -> render -> reconcile -> Git 的单一路径、进程内互斥锁、`sync_if_idle()` 非阻塞入口、失败边界和 `SyncResult`。完成后执行独立 spec/code-quality review。

后续顺序：

- Task 9：SQLite/WAL/SHM watcher、防抖、pending rerun。
- Task 10：service lifecycle、启动/周期同步、SIGINT/SIGTERM。
- Task 11：setup.py 和 launchd plist preview。
- Task 12：临时 Git 仓库端到端集成测试。
- Task 13：README 与 Agent.md 对齐。
- Task 14：compileall、全套测试、禁止操作扫描、whole-branch review。

每个任务必须有 brief、记录 BASE、implementer report、review package 和独立双 verdict review。

## 6. 绝对不要踩的坑

### Git 和分支

- 当前在 `main`，不要擅自新建嵌套 worktree，也不要切换到旧实现 worktree。
- 不要 `git reset --hard`、rebase、amend、force push、删除分支或清理工作区。
- 不要使用 `git clean -fdx`，尤其不能清理 `.superpowers/sdd/`、`.claude/worktrees/` 或 `.DS_Store`。
- 不要使用 `git add .` 或不带明确路径的 commit。
- 不要把无关的 `.gitignore`、`docs/CHANGELOG.md`、`.DS_Store`、HANDOFF 文件或 nested worktree 状态带入功能提交。
- 不要把 `HEAD~1` 当 task review BASE；必须使用实际 dispatch 前记录的 BASE。
- 未经用户明确要求，不要 push、创建 PR、修改远程或加载 launchd。

### SDD 流程

- implementer 自测不是正式 review；正式 review 必须分别给出规格合规和代码质量 verdict。
- reviewer 的 Critical/Important finding 必须进入修复循环；controller 不直接修改相关实现。
- 修复后必须重新生成 scoped package 和独立复审，不能因改动很小而跳过。
- Minor finding 不能静默丢弃；应记录为 deferred 或修复。
- review agent 失败后不要虚构结论；重新派发并记录真实原因。
- 未完成 Task 7 review 前不能开始 Task 8。

### Things3 只读边界

- 业务代码只能通过 `things.py` 的窄读取接口访问 Things。
- 不要读取/写入 Things SQLite 私有 schema。
- 不要导入或调用 `complete()`、`url()` mutation helper。
- 不要构造 `update`、`update-project` 或其他写入 URL。
- 不要把 `things.py` 文档中的 `Task`、`Project`、`Area` 当作公共类。
- 不要把 `trashed` 自动映射为 canceled。
- 不要输出公开 API 无法可靠提供的 priority、Evening、附件或重复规则字段。
- Today 必须复用 `things.today()` 语义；不能只根据 `start` 字段自行重建。
- 完整快照读取成功之前，不得修改目标仓库或运行 Git。

### 文件系统和路径

- 生产文件操作使用 `pathlib.Path`。
- 写文件必须使用同目录临时文件、flush、fsync、`os.replace()`；失败时保留原文件。
- 任何 desired destination、祖先、manifest destination 在写入前都要做 ownership preflight。
- 绝不能替换未标记陌生文件或符号链接；悬空符号链接也算已存在的 unowned entry。
- manifest 必须严格按 version 1 schema 校验；坏 JSON、坏 UTF-8、错误类型、未知字段、危险路径都要在任何写入前失败。
- 只删除 marker/manifest 明确管理的文件；陌生文件必须保留并 warning。
- 归档不能递归移动含陌生文件的整个目录；只处理已管理文件。
- 路径必须是相对路径，禁止绝对路径和 `..`；本项目路径语义明确采用 macOS/POSIX，标题反斜杠是普通字符。
- `Archived/` 是持久历史区，不属于普通当前文件 stale deletion 集合。

### Git 实现

- 生产同步必须用临时 `GIT_INDEX_FILE`，不能把用户原有 staged 文件带进同步提交。
- 只使用明确 pathspec，不要 `git add .`。
- Git 命令必须有 timeout、捕获 stderr，日志中的 URL userinfo/token/auth-token/password 必须脱敏。
- 推送失败不能自动 pull、merge、rebase 或处理冲突。
- 只根据 `refs/things3-sync/pending-push` 判断同步器待推送提交，不要因为分支一般领先远端就推送用户自己的提交。
- 不要对真实远端执行 push。

### 测试和环境

- 优先使用 `/tmp/things3-task3-venv/bin/python`，系统 Python 可能没有 pytest。
- 推荐命令：

```bash
/tmp/things3-task3-venv/bin/python -m pytest tests -v
```

- 如果执行根目录 broad `pytest`，可能误收集被忽略的 `things.py/tests`，出现 `ModuleNotFoundError: tests.test_things`；这不是项目 `tests/` 套件失败。报告中要区分两者。
- 测试时避免产生不必要的 bytecode/cache；可用 `PYTHONDONTWRITEBYTECODE=1 python3 -B -m pytest -p no:cacheprovider ...`。
- 每次任务的报告要记录 RED/GREEN 命令、精确输出和未执行的检查。

## 7. 继续开发的启动步骤

新对话应按以下顺序操作：

1. 阅读本文件。
2. 阅读 `Agent.md`、设计规格、实施计划和 `.superpowers/sdd/2026-09-04-things3-sync-mvp/progress.md`。
3. 执行 `git status --short`、`git log --oneline --decorate -10`，确认当前 HEAD 与未提交文件；不要清理任何既有状态。
4. 阅读 `task-7-brief.md`、`task-7-report.md`、`review-task-7-complete.diff`。
5. 重新派发 Task 7 独立 reviewer；若额度仍不足，使用更低成本模型，但保留双 verdict 要求。
6. 完成并复审 Task 7 后，更新账本，再开始 Task 8。
7. 后续每个任务严格执行 brief -> BASE -> implementer -> report -> review -> fix/re-review -> ledger 的顺序。

本交接文件不属于任何功能提交，除非用户明确要求提交它。
